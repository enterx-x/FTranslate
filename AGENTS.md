# AGENTS.md

本文件是 FTranslate 项目的协作与执行规则，适用于整个仓库。

## 0. 优先级

1. 系统、开发者、用户在当前对话中的明确指令优先于本文件。
2. 本文件优先于临时习惯和个人偏好。
3. 如果规则之间冲突，先暂停并说明冲突点，不要用沉默假设绕过。

## 1. 每次操作前的固定入口

每次开始任何项目操作前，必须先浏览：

1. `README.md`
2. `PLAN.md`
3. `DESIGN.md`

如果 `PLAN.md` 不存在，先创建或恢复一个最小版本，再继续工作。
如果 `DESIGN.md` 不存在，先创建或恢复一个最小版本，再继续工作。

阅读目的：

- 确认当前产品定位、架构边界和长期计划；
- 确认当前 UI/交互设计基准、布局模式、颜色系统和禁止重复的视觉问题；
- 确认最近遇到的问题、失败原因和禁止重复犯错的事项；
- 避免脱离计划、重复造轮子或把短期修补误当成长期方向。

## 2. 开发类活动：第一性原理

进行任何开发类活动时，必须从第一性原理出发，而不是从现有 UI、已有模块或短期实现便利倒推。

必须先回答：

1. 用户或目标场景的真实问题是什么？
2. 这个问题的不可压缩核心约束是什么？
3. 当前实现中哪些能力可以复用，哪些只是历史包袱？
4. 最小可验证闭环是什么？
5. 如何证明改动真的解决了问题，而不是只是增加功能入口？

开发类活动包括但不限于：

- 新功能设计；
- 架构调整；
- UI/交互改造；
- 数据模型变更；
- AI prompt、Agent、RAG、工作流设计；
- 实验、复现、模型运行和本地工具链集成。

## 3. 审查类活动：对抗式审查

进行审查类工作时，必须采用对抗式审查。

审查目标不是寻找可以表扬的地方，而是主动寻找：

- 需求是否被误解；
- 是否存在错误假设；
- 是否引入隐性状态或不可追踪行为；
- 是否破坏现有工作流；
- 是否缺少测试、验证或回滚路径；
- 是否有安全、隐私、数据损坏、性能或可维护性风险；
- 是否只是包装概念，没有形成真实可用闭环。

审查输出必须优先列问题和风险，再给总结。没有发现问题时，也要明确说明剩余风险和未覆盖验证。

## 4. 优先复用优秀开源项目

遇到通用能力时，优先评估并复用成熟、维护良好、许可证清晰的开源项目，而不是重复造轮子。

适合优先复用的能力包括但不限于：

- PDF 解析、OCR、版面分析；
- 文档、表格、PPT 处理；
- 向量索引、embedding、RAG；
- 工作流编排；
- 实验追踪、日志解析、可视化；
- 模型运行、微调、推理服务；
- 代码仓库分析、语法解析；
- 图谱布局、数据可视化。

引入开源依赖前必须评估：

1. 是否真正减少复杂度；
2. 是否与 Electron + React + TypeScript + 本地 Python sidecar 架构兼容；
3. license 是否允许当前用途；
4. 维护活跃度和生态稳定性；
5. 是否会显著增加安装体积、构建复杂度或 Windows 兼容风险；
6. 是否存在更小、更稳、更贴近现有架构的替代方案。

## 5. 代码修改后的同步要求

每次进行代码修改时，必须同步完成以下事项：

1. 更新 `README.md`，说明用户可感知的能力、运行方式、配置变化或验证方式。
2. 更新 `PLAN.md`，记录本次改动对应的计划项、遇到的问题、已验证结果和后续风险。
3. 运行与改动范围匹配的最小验证命令，例如 `npm test`、`npm run typecheck`、`npm run build` 或 `npm run visual:check`。
4. 查看 `git diff`，确认没有无关修改、敏感信息或生成物进入提交范围。
5. 进行 git 提交并推送到远端。

如果因为网络、权限、远端配置或用户明确要求导致无法提交或推送，必须：

- 在最终回复中明确说明未完成的 git 步骤；
- 在 `PLAN.md` 的问题台账中记录原因和下一步处理方式；
- 不得声称已经完成同步。

文档-only 修改不强制触发完整构建，但仍需检查 diff，并说明未运行代码验证的原因。

## 6. README.md 与 PLAN.md 的职责

`README.md` 面向用户和演示：

- 产品定位；
- 主要功能；
- 安装、运行、打包；
- 关键配置；
- 已验证流程；
- 常见问题。

`PLAN.md` 面向项目执行：

- 长期产品方向；
- 当前阶段目标；
- 已做/待做计划；
- 设计决策记录；
- 遇到的问题和根因；
- 禁止重复犯错清单；
- 验证命令和结果记录。

不要把详细执行计划只写在聊天记录里。只要影响后续开发判断，就必须沉淀到 `PLAN.md`。

## 7. 当前产品方向

当前长期方向不是单纯的 PDF 翻译器，而是：

> 面向高校实验室、AI 创业团队和园区科创企业的本地化 AI 科创研发工作台。

核心目标是把论文、代码、实验、模型运行状态和研发知识沉淀为可追踪、可验证、可复用的研发闭环。

未来设计应优先围绕：

- AI 科创项目空间；
- Paper-to-Method 论文方法编译；
- Paper-to-Code 复现映射；
- 实验矩阵与消融设计；
- 本地 AI Runtime Center；
- 证据知识图谱；
- Research Autopilot 多智能体研发推进。

任何新增功能都要说明它如何服务这个闭环。
