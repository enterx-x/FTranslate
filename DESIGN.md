# FTranslate DESIGN.md

## iPhone 本地论文阅读子产品

移动端是 FTranslate 的独立阅读表面，不复刻桌面 App Shell。首版服务的物理场景是：研究者离开工作站后，在通勤、会议间隙或实验现场用单手完成“找论文—存本地—连续阅读—查看中文”的短闭环。

### 信息架构

- 底部仅保留“论文库 / 检索 / 阅读”三个入口；没有首页、实验矩阵、知识图谱、Runtime Center 或设置页。
- 论文库是默认入口，继续阅读优先于统计指标；空状态只提供本地导入和前往检索两个动作。
- arXiv 结果保持单列，结果对象包含领域、日期、标题、摘要、作者和“存入论文库”动作，不使用桌面多列卡片密度。
- 阅读器只提供“连续双语 / 原始 PDF”两种清晰模式。上传普通 PDF 后默认进入连续双语，不再要求用户另行制作或导入双语 PDF；原始 PDF 始终保留用于版式核验。

### 翻译交互

- 全文按原页序重排为单一纵向文章流，不能用上一页/下一页分页打断；滚动位置同步为原 PDF 页码，便于返回时继续阅读。
- 完整段落译文必须直接出现在对应英文段落下方，使用轻量左侧结构线区分，不以 modal、底部抽屉或悬浮窗打断连续阅读。
- 选词或短语翻译只有在用户确实选择文本后才显示轻量浮层；浮层必须限制在视口内，长英文单行截断，不能覆盖关闭按钮。
- “翻译全文/翻译剩余”按段落逐条生成并立即写入本机缓存，允许在当前段结束后停止；失败必须保留原文与已成功译文并显示真实错误，不能用空译文伪装成功。
- 当 PDF 没有文字层时，不能只显示死路提示。应提供“识别并生成双语”，将页面按移动内存上限渲染后逐页发送到用户配置的图片模型，一次生成原文与中文；每页完成后同时保存段落、译文和识别进度，允许停止、失败重试和重新打开后续跑。
- 扫描识别入口必须说明页面图片会发往用户选择的模型服务商并可能产生按页费用；FTranslate/Vercel 不保存页面图片。模型不支持图片或没有返回严格段落结构时显示真实错误，不回退为伪造文本。
- API Key 不写入 Preferences 或 localStorage，只在当前 App 运行会话内存在；Base URL 和模型名允许本地持久化。

### 视觉与设备约束

- 以 390×844 为主要 iPhone 验证尺寸，同时覆盖 430×932；所有顶部、底部固定区域必须使用 `safe-area-inset-*`。
- 主体保持冷调浅色实体表面、深石墨操作色和低饱和蓝灰结构色；不使用桌面三栏、嵌套卡片、营销 hero、玻璃拟态或大面积渐变。
- 论文英文正文使用适合长文的 serif，中文译文使用系统 sans-serif，二者通过背景与语言标签区分但保持同一阅读节奏。
- 页面级 `scrollWidth` 不得大于视口宽度；PDF 画布内部允许受控缩放和滚动，但不能把横向滚动泄漏到 App Shell。
- 原始 PDF 必须同时提供可发现的“适宽 / 减小 / 放大”控件和双指缩放；双指缩放围绕两指中心保持阅读锚点，单指仍用于滚动画布。

### 数据与分发边界

- PDF 文件写入 Capacitor Filesystem 的 App 沙盒；论文索引使用 Capacitor Preferences；段落翻译缓存按论文写入独立 JSON 文件。
- arXiv 网络请求和 PDF 下载使用 Capacitor 原生 HTTP/File Transfer，避免把 Electron IPC 或 Node API 带入 WKWebView。
- 当前不实现任何账号、云端数据库或三端同步，只保留稳定的 `MobilePaper`、`MobileStoredPdf` 与翻译缓存标识，供后续同步映射。
- 不走 App Store。交付为已签名的 Ad Hoc `.ipa`，只允许 Provisioning Profile 中登记的 iPhone 安装；HTTPS 安装页是分发外壳，不绕过 Apple 签名与设备登记约束。

本文件定义 FTranslate 未来 UI 的设计语言。它面向 AI coding agent 和后续人工开发，不以当前界面为最终参考。

## 1. 产品气质

FTranslate 不是普通 PDF 翻译器，也不是营销型 AI 聊天入口。它的目标是本地化 AI 科创研发工作台，帮助高校实验室、AI 创业团队和科创企业把论文、代码、实验、模型运行状态和研发知识沉淀成可追踪、可验证、可复用的研发闭环。

界面应当像专业研发控制台，而不是展示页。优先级是：

1. 信息密度和可扫描性；
2. 证据来源和状态可追踪；
3. 长时间阅读、整理、复现和调试的舒适度；
4. 可解释的工作流闭环；
5. 克制、稳定、工程化的视觉表达。

## 2. 参考系统

从 `awesome-design-md` 中采用组合参考，而不是照搬单一品牌：

- IBM / Carbon：主设计底座。用于工作台、表格、表单、设置、Runtime 状态、实验矩阵。
- Mintlify：文档和证据阅读结构。用于 Paper-to-Method、Paper-to-Code、方法卡、证据链页面。
- Supabase：产品工作台面板表达。用于项目空间、任务队列、日志、模型状态和数据面板。
- Cursor：AI 研发流程表达。用于 Agent 执行 timeline、代码复现映射、调试进度。
- Linear：只借鉴 App Shell 的克制质感、状态 badge、细边框和有限 accent，不采用全暗色主界面。

不建议作为主参考：

- ClickHouse、VoltAgent、Sentry：品牌色太强，不适合作为科研桌面工作台底座。
- OpenCode、Ollama：README / 终端感过强，不适合复杂中文科研阅读和实验矩阵。
- Replicate、Vercel：可局部借鉴，但整体更偏营销页或开发者平台宣传。

## 3. 视觉原则

### 3.1 主界面

- 主工作区使用浅色、高对比、低噪声画布。
- 左侧导航可以保留深色 App Shell，但不能让整套产品变成黑色科技风。
- 大部分页面应由表格、列表、详情面板、证据卡、状态条和操作栏构成。
- 页面首屏要直接进入工作流，不做营销式 hero。
- UI 文字要服务操作，不写解释性宣传文案。

### 3.2 信息层级

每个页面必须明确四层信息：

1. 当前对象：项目、论文、代码仓库、实验、模型或任务；
2. 当前状态：未开始、处理中、已完成、失败、需要人工确认；
3. 证据来源：PDF 页码、段落、公式、图表 caption、代码文件、日志片段；
4. 下一步动作：继续阅读、映射代码、生成方法卡、设计实验、运行检测、导出。

不要只展示“功能入口”。入口必须连接到研发对象和下一步动作。

## 4. 颜色系统

颜色应少而稳定，避免大面积渐变、多彩装饰和高饱和塑料感。主界面优先使用石墨灰、冷灰白和低饱和语义色；亮蓝、亮绿、亮黄只能作为极小面积强调，不允许成为状态 badge 或面板底色的默认视觉语言。

### 4.1 推荐 token

```css
:root {
  --ft-canvas: #f7f8fa;
  --ft-surface: #ffffff;
  --ft-surface-muted: #f2f4f7;
  --ft-surface-raised: #ffffff;
  --ft-sidebar: #0b0f17;
  --ft-sidebar-raised: #111827;
  --ft-ink: #161616;
  --ft-ink-muted: #525866;
  --ft-ink-subtle: #7a8494;
  --ft-border: #d9dee7;
  --ft-border-soft: #e7ebf1;
  --ft-primary: #0f62fe;
  --ft-primary-hover: #0050e6;
  --ft-primary-soft: #e8f0ff;
  --ft-success: #24a148;
  --ft-warning: #f1c21b;
  --ft-danger: #da1e28;
  --ft-info: #0f62fe;
}
```

### 4.2 使用规则

- 主操作优先使用深石墨色或克制蓝灰色；亮蓝只允许用于少量链接、焦点和明确选中态。
- 左侧 App Shell 选中态优先使用石墨底、冷灰边框和低饱和蓝灰指示线；禁止回到高饱和紫蓝描边或霓虹式 inset shadow。
- 成功、可用、已完成使用低饱和灰绿，不使用荧光绿或糖果绿。
- 警告、等待人工确认和风险提示使用低饱和雾灰蓝 / 石板灰，不使用高亮黄色块或棕色旧纸色。
- 红色只用于错误、失败、危险操作。
- 紫色、橙色、青色等可以出现在图表或标签中，但不能成为主界面品牌色。
- 不使用大面积紫蓝渐变、霓虹、光球、玻璃拟态背景。

## 5. 字体和排版

FTranslate 主要面向中文科研使用，排版必须优先照顾中文可读性。

推荐字体：

```css
font-family:
  "Microsoft YaHei UI",
  "Microsoft YaHei",
  "Segoe UI",
  "PingFang SC",
  "Noto Sans CJK SC",
  system-ui,
  sans-serif;
```

代码、路径、命令、模型 ID、论文 ID 使用等宽字体：

```css
font-family:
  "JetBrains Mono",
  "Cascadia Mono",
  "SFMono-Regular",
  Consolas,
  monospace;
```

排版规则：

- 正文默认 14px 或 15px，行高 1.45-1.6。
- 表格、列表、详情面板可以使用 12px-13px 元信息。
- 页面标题控制在 24px-32px，不使用巨型营销标题。
- 工作台卡片标题控制在 16px-20px。
- 不使用 viewport width 动态缩放字体。
- 中文按钮文字不要过长，必要时换成图标 + tooltip。

## 6. 布局模式

### 6.1 App Shell

默认结构：

```text
┌──────────────┬────────────────────────────────────────────┐
│ Left Nav     │ Top context bar / page actions             │
│              ├──────────────────────┬─────────────────────┤
│              │ Main work area        │ Detail / evidence   │
│              │ list/table/canvas     │ panel               │
│              ├──────────────────────┴─────────────────────┤
│              │ Status queue / runtime message              │
└──────────────┴────────────────────────────────────────────┘
```

左侧导航只负责模块切换，不承载复杂说明。右侧详情面板用于当前选中对象的证据、状态、操作和日志。

### 6.2 典型页面

项目空间首页：

- 左：项目概览、最近论文、运行中任务；
- 中：研发流程看板，按 Paper-to-Method、Paper-to-Code、Experiment Matrix、Runtime Center 展示少量可执行对象卡；
- 右：Current Focus / Next Actions / Decision Queue Inspector，用于当前焦点、下一步和风险缺口。
- 项目 KPI 优先合并为单个分段指标块，而不是拆成多张小卡；指标服务扫描，不承担视觉装饰。
- 1366px、1440px 和常见矮窗口下，Workflow Board、三条 Next Actions 和 Decision Queue 必须首屏可读；可以压缩说明文字，但不能让动作行被下一段标题或风险面板裁切。

Paper-to-Method：

- 左：论文章节 / 图表 / 公式目录；
- 中：方法卡结构化字段；
- 右：证据来源和原文片段。

Paper-to-Code：

- 左：代码仓库文件树和关键入口；
- 中：论文模块到代码文件的映射表；
- 右：依赖、运行命令、错误日志和风险。

实验矩阵：

- 顶部：方法卡桥接摘要，展示当前项目方法卡、可合并实验行和证据定位数；
- 主体：高密度表格；
- 工具栏：实验组、seed、metric、状态筛选；
- 右侧：选中实验的配置、日志、结果和失败诊断。
- 1366px / 1440px 宽度下主表优先保留实验组、论文、假设、方法和指标；状态按钮和证据定位交给右侧详情面板，不为了展示全字段而裸露底部横向滚动条。

Runtime Center：

- 主体：本地模型、sidecar、任务队列、缓存和 GPU/CPU 状态；
- 右侧：故障详情、修复命令、路径检测结果。

AI Autopilot：

- 主体：Agent timeline；
- 右侧：每一步输入、输出、证据和写入位置；
- 底部：人工确认队列。

## 7. 组件规则

### 7.1 按钮

- 主按钮：深石墨或克制蓝灰底白字，小圆角 4px-6px，高度 32px-40px；不要使用高饱和亮蓝作为默认 CTA。
- 次按钮：白底或浅灰底，1px 边框，黑色文字。
- 危险按钮：红色只用于删除、覆盖、清理、重置等危险操作。
- 工具按钮优先用图标，配 tooltip。
- 不使用大面积 pill CTA。状态 badge 可以使用 pill。

### 7.2 卡片和面板

- 卡片只用于重复对象、详情面板、状态面板和 modal。
- 页面区域不要套卡片再套卡片。
- 工作台首页、项目空间和 Runtime Center 可以使用卡片化 dashboard，但卡片必须对应项目、论文、流程阶段、下一步动作、风险或运行状态等真实研发对象。
- 工作台首页允许使用研发流程看板对象卡，但卡片必须对应真实研发对象或流程阶段，具备状态和动作；不能把指标、行动项和风险缺口都拆成装饰性卡片。
- 首页 KPI、短摘要和动作队列优先使用分段块、列表行和连续 Inspector；只有真实流程对象、风险缺口或可执行任务才使用明确卡片。
- 反对卡片堆叠不等于把界面压平成粗糙表格；顶层 pane 可以保留克制圆角、边框、浅阴影和留白，内部对象再用分隔线、状态 badge、行 hover 和小型操作按钮组织。
- 如果用户明确要求恢复卡片式工作台，优先采用“少量大面板 + 真实对象卡 + 右侧任务卡”的方式，避免灰条标题、硬表格边框和卡片内再套厚重卡片。
- 默认边框 1px，轻阴影可选但必须克制。
- 推荐圆角 6px-8px；大型面板最多 12px。
- 卡片内必须有清晰对象、状态和可执行动作。

### 7.3 表格和列表

- 表头使用浅灰背景或底部分割线。
- 行 hover 只做轻微背景变化。
- 每行尽量展示状态、来源、最近动作和下一步入口。
- 支持密度切换时，必须保证文字不截断关键字段。

### 7.4 标签和状态

状态标签必须语义稳定：

- `Draft` / `Ready` / `Running` / `Blocked` / `Failed` / `Verified`
- `Evidence missing` / `Needs review` / `Local only` / `Cached`

标签不能只用于装饰。每个标签都应该帮助用户判断风险或下一步。状态 badge 应使用低饱和底色、细边框和深色文字；不要用亮蓝、亮绿、亮黄做大面积 pill，因为这会让科研工作台显得廉价和塑料。

### 7.5 证据卡

证据卡必须显示：

- 来源类型：PDF、Figure、Table、Equation、Code、Log、Experiment；
- 来源定位：页码、文件路径、函数名、行号或日志时间；
- 摘要；
- 操作：打开来源、复制引用、加入方法卡、加入实验矩阵。

### 7.6 动效

动效服务研发控制台的层级、状态和反馈，不服务炫技展示。

- 主视图切换允许使用 220ms-280ms 的 fade + translate + 轻 scale，不能超过 350ms。
- 页面顶层 pane、右侧详情、表格和图谱进入可以使用 30ms-90ms stagger，但必须只作用于 opacity / transform / filter，不能改变布局尺寸。
- 按钮、导航、对象卡、表格选中行和详情面板 hover 只允许 1px-2px 位移、低饱和边框变化和轻阴影。
- Ready / Planned / Draftable 等状态 badge 可以在 hover、选中或状态变化时出现一次性 sheen / shimmer；禁止持续闪烁、霓虹呼吸和高饱和流光。
- 知识图谱节点允许进入 scale-in，选中节点允许低饱和 halo 呼吸，边线 hover / active 允许平滑变深；禁止大面积发光、粒子和持续移动背景。
- 检索页和长列表页可以使用结果卡 stagger、搜索状态扫描线和详情抽屉进入动画，但必须保持 200ms-360ms 内完成，并且不能改变卡片实际布局尺寸。
- 长列表结果卡应优先使用 CSS containment / `content-visibility` 等浏览器原生性能隔离；如果后续需要虚拟列表，必须保留键盘可达性、选中态和右侧详情同步。
- 不默认引入 GSAP。只有在后续确实需要复杂时间线、拖拽编排或大规模 motion choreography 时，再单独评估依赖、体积、license 和 Windows 打包风险。
- 必须支持 `prefers-reduced-motion: reduce`，降级后不能有持续动画、闪烁或影响阅读的过渡。

## 8. AI 和可追踪性

任何 AI 生成内容都不能只是聊天气泡。界面必须显示：

- 生成对象；
- 输入来源；
- 证据覆盖率；
- 未确认字段；
- 写入位置；
- 回滚或编辑入口。

Agent 执行过程应展示为 timeline：

```text
Reading paper -> Extracting method -> Mapping code -> Designing experiment -> Checking runtime -> Awaiting review
```

每个阶段都要有状态、耗时、证据和失败原因。

## 9. 响应式和桌面约束

FTranslate 是 Windows Electron 桌面应用，优先桌面体验。

- 默认设计宽度以 1366px、1440px、1920px 为主。
- 1280px 以下允许折叠右侧详情面板。
- arXiv、论文导师、PDF 阅读等双栏/三栏页面的折叠态不能只隐藏内容而保留空白列；折叠后必须把主工作区扩展到窄 rail 前。
- 不以移动端为主，但窗口缩小时不能横向溢出。
- 表格、PDF、知识图谱这类核心区域必须使用稳定尺寸约束。
- 视觉回归必须覆盖首页、项目空间、表格、PDF 阅读、AI 问答、Runtime Center 和设置页。

## 10. 禁止项

- 不以当前 UI 为最终视觉参考。
- 不做营销型 landing page。
- 不做全局暗色科技风。
- 不用大面积渐变、光球、玻璃拟态、霓虹边框。
- 不用巨型 hero 文案替代真实工作流。
- 不把 AI 输出停留在聊天记录里。
- 不用装饰性卡片堆叠掩盖缺少数据闭环。
- 不把“减少卡片”误解为廉价线框表格；视觉密度、层级、留白和操作焦点必须同时成立。
- 不隐藏失败、跳过错误或用成功文案掩盖未验证状态。

## 11. 分阶段落地

第一阶段：设计系统基线

- 建立 `DESIGN.md`。
- 梳理全局颜色、间距、按钮、卡片、表格、状态标签。
- 保持现有功能可用，不大规模重构。

第二阶段：项目空间和导航

- 将首页改为 AI 科创项目空间。
- 左侧导航围绕项目、论文、代码、实验、Runtime、证据图谱组织。
- 当前旧入口以兼容方式保留。

第三阶段：核心研发对象

- Paper-to-Method 方法卡；
- Paper-to-Code 复现映射；
- 实验矩阵；
- Runtime Center；
- Evidence Graph。

第四阶段：Research Autopilot

- 多 Agent timeline；
- 人工确认队列；
- 输出写回项目空间和证据链。

## 12. 验证方式

每次 UI 改动后至少执行：

```bash
npm run typecheck
npm run visual:check
```

视觉检查后必须人工查看 `.tmp-visual-check/` 中相关截图，按对抗式审查确认：没有重叠、遮挡、截断、横向/纵向溢出、裸露内部滚动条、流程对象卡压缩、流程卡标题/说明/动作重叠、下一步动作裁切、风险行裁切、实验矩阵方法卡桥接带换行截断、实验矩阵 1366px 表格底部横向滚动条、亮蓝/亮绿/亮黄塑料状态色回潮、侧栏选中态高饱和紫蓝回潮、实验矩阵主按钮和 badge 高饱和回潮、紫色品牌色回潮、过度卡片化或过度线框化。

如果只修改文档，可以不运行代码测试，但必须执行：

```bash
git diff -- README.md PLAN.md DESIGN.md
```

并在 `PLAN.md` 记录设计决策和验证结果。

## 13. 旧页面 UI 收口补充

旧页面继续优化时，优先处理视觉系统一致性，而不是重做营销式首页或继续添加装饰卡片。

- 设置、AI 助手、论文导师、组会 PPT、知识图谱和 arXiv 检索都应使用同一套低饱和中性面板、深石墨主按钮、灰蓝边框和克制阴影。
- 低饱和不等于全灰黑。页面必须保留可辨认的低饱和方向色，例如蓝灰、鼠尾草绿、雾灰蓝、石板灰或灰紫，但这些颜色只能用于页面方向、状态线、图谱节点、active 状态和轻量提示，不做大面积高饱和色块，也不使用明显棕色作为默认强调色。
- 高饱和蓝、绿、黄、紫不得作为默认 badge、状态块、空状态光晕、头部渐变或大面积面板底色。
- 设置页、图谱页和其他内部导航的 active 状态不能只使用纯黑或纯灰块，必须有低饱和色相，并通过视觉脚本检查其颜色通道差没有坍缩为灰度。
- arXiv 等检索页在有结果时，辅助队列只能作为摘要条或右侧详情入口，不能展开遮挡结果卡片；空结果时可以展示候选列表，但必须保持单列可读、无遮挡、无横向滚动。
- 空状态可以保留轻边框和留白，但不要用紫蓝渐变、光球、霓虹阴影或大面积品牌色制造“高级感”。
- 每次触碰上述页面样式后，`visual:check` 必须继续检查高饱和回潮，并人工查看 `.tmp-visual-check/` 中相关截图。

## 14. 聚焦型 Research OS 材质与动效补充

- 首页、PDF 阅读器和 AI 助手共享冷调浅色画布、深石墨文字、蓝灰结构色和蓝紫焦点色。
- 允许紫蓝渐变、局部玻璃材质和柔和发光，但同一视图只保留一个主要发光焦点；普通工作面板继续使用实体表面。
- 侧栏按“项目概览、论文与阅读、方法与证据、实验与运行、AI 研究助手、组会与导出”分组，选中态保持低饱和。
- PDF 画布始终高于装饰层；右侧检查器可使用玻璃材质，但不能降低文字对比或遮挡文档。
- AI 持续动效只在真实生成期间存在，完成、失败或空闲状态必须收敛为静态。
- 页面切换、面板进入、hover 和 active 反馈使用 180ms-320ms 的 transform、opacity、filter 与阴影变化，并支持减少动画模式。
